{
  "name": "kiddo-adventures",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build",
    "deploy": "npm run build && npx wrangler deploy --config worker/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
