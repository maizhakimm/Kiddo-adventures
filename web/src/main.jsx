import React,{useEffect,useMemo,useRef,useState}from'react';
import{createRoot}from'react-dom/client';
import{makeChallenge}from'./questionBank.js';
import'./styles.css';

const API='';
const PRICE=29;
const AVATARS=[
  {id:'panda',icon:'🐼',label:'Panda'},
  {id:'kucing',icon:'🐱',label:'Kucing'},
  {id:'beruang',icon:'🐻',label:'Beruang'},
  {id:'arnab',icon:'🐰',label:'Arnab'},
  {id:'musang',icon:'🦊',label:'Musang'},
  {id:'gajah',icon:'🐘',label:'Gajah'}
];
const avatarIcon=id=>AVATARS.find(a=>a.id===id)?.icon||'🧒';
const worldMeta={
  huruf:{icon:'🔤',world:'Kota Abjad',tag:'Bunyi • Huruf • Perkataan',tone:'sunset',scene:'🏝️  🌊  🐚'},
  nombor:{icon:'🔢',world:'Kota Nombor',tag:'Kira • Susun • Banding',tone:'sky',scene:'🚂  🌤️  ⛰️'},
  warna_bentuk:{icon:'🎨',world:'Hutan Pelangi',tag:'Warna • Bentuk • Corak',tone:'violet',scene:'🌳  🌈  🦋'},
  padan_gambar:{icon:'🧩',world:'Teluk Ingatan',tag:'Ingatan • Fokus • Padanan',tone:'mint',scene:'🛶  🐠  🐚'},
  jigsaw:{icon:'🧸',world:'Pulau Teka-Teki',tag:'Visual • Ruang • Sabar',tone:'yellow',scene:'🏰  🧸  ⭐'},
  kira_asas:{icon:'➕',world:'Gunung Matematik',tag:'Tambah • Tolak • Logik',tone:'rose',scene:'🚗  🛣️  🏔️'}
};

async function api(path,options={}){
  let token='';
  try{token=JSON.parse(localStorage.getItem('kiddo_parent'))?.token||''}catch{}
  const headers={'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`} :{}),...(options.headers||{})};
  const r=await fetch(`${API}${path}`,{...options,headers});
  const d=await r.json().catch(()=>({}));
  if(!r.ok)throw new Error(d.error||'Ralat sambungan');
  return d;
}

let audioCtx;
function playSfx(type,enabled=true){
  if(!enabled||typeof window==='undefined')return;
  try{
    const Ctx=window.AudioContext||window.webkitAudioContext;
    if(!Ctx)return;
    audioCtx=audioCtx||new Ctx();
    if(audioCtx.state==='suspended')audioCtx.resume();
    const notes=type==='correct'?[523.25,659.25,783.99]:type==='finish'?[523.25,659.25,783.99,1046.5]:[220,174.61];
    notes.forEach((freq,i)=>{
      const osc=audioCtx.createOscillator();
      const gain=audioCtx.createGain();
      osc.type=type==='wrong'?'triangle':'sine';
      osc.frequency.value=freq;
      gain.gain.setValueAtTime(.0001,audioCtx.currentTime+i*.11);
      gain.gain.exponentialRampToValueAtTime(.18,audioCtx.currentTime+i*.11+.02);
      gain.gain.exponentialRampToValueAtTime(.0001,audioCtx.currentTime+i*.11+.16);
      osc.connect(gain);gain.connect(audioCtx.destination);
      osc.start(audioCtx.currentTime+i*.11);osc.stop(audioCtx.currentTime+i*.11+.18);
    });
  }catch{}
}

function Modal({title,onClose,children}){return <div className="overlay" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal"><button className="x" onClick={onClose}>×</button><h2>{title}</h2>{children}</div></div>}
function AuthModal({onClose,onLogin,refCode}){const[mode,setMode]=useState('login');const[form,setForm]=useState({email:'',password:'',name:'',agent_code:refCode||''});const[busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function submit(e){e.preventDefault();setBusy(true);setMsg('');try{const d=await api(mode==='login'?'/api/login':'/api/signup',{method:'POST',body:JSON.stringify(form)});localStorage.setItem('kiddo_parent',JSON.stringify(d));onLogin(d)}catch(err){setMsg(err.message)}finally{setBusy(false)}}return <Modal title={mode==='login'?'Log Masuk Ibu Bapa':'Daftar Akaun Ibu Bapa'} onClose={onClose}><div className="switch"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Log Masuk</button><button className={mode==='signup'?'active':''} onClick={()=>setMode('signup')}>Daftar</button></div><form className="form" onSubmit={submit}>{mode==='signup'&&<input required placeholder="Nama ibu / bapa" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>}<input type="email" required placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input type="password" required minLength="6" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{mode==='signup'&&<input placeholder="Kod agent (jika ada)" value={form.agent_code} onChange={e=>setForm({...form,agent_code:e.target.value.toUpperCase()})}/>} {msg&&<div className="alert">{msg}</div>}<button className="primary" disabled={busy}>{busy?'Tunggu…':mode==='login'?'Masuk':'Cipta Akaun'}</button></form></Modal>}
function ParentGate({parent,onClose,onVerified}){const[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function verify(e){e.preventDefault();setBusy(true);setMsg('');try{await api('/api/login',{method:'POST',body:JSON.stringify({email:parent.email,password})});onVerified()}catch{setMsg('Password ibu bapa tidak betul.')}finally{setBusy(false)}}return <Modal title="Kawasan Ibu Bapa 🔐" onClose={onClose}><p>Masukkan semula password ibu bapa untuk mengubah atau memadam profil anak.</p><form className="form" onSubmit={verify}><input autoFocus type="password" required minLength="6" placeholder="Password ibu bapa" value={password} onChange={e=>setPassword(e.target.value)}/>{msg&&<div className="alert">{msg}</div>}<button className="primary" disabled={busy}>{busy?'Semak…':'Buka Tetapan Profil'}</button></form></Modal>}
function ProfileEditor({parent,profile,onClose,onSaved}){const isEdit=!!profile;const[form,setForm]=useState({name:profile?.name||'',age:String(profile?.age||''),avatar:profile?.avatar||'panda'});const[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function save(e){e.preventDefault();setBusy(true);setMsg('');try{const body={...form,age:Number(form.age)};if(isEdit)await api(`/api/child-profiles/${profile.id}`,{method:'PUT',body:JSON.stringify(body)});else await api('/api/child-profiles',{method:'POST',body:JSON.stringify(body)});await onSaved();onClose()}catch(err){setMsg(err.message)}finally{setBusy(false)}}return <Modal title={isEdit?'Urus Profil Anak':'Tambah Profil Anak'} onClose={onClose}><form className="form" onSubmit={save}><input required placeholder="Nama anak" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><select required value={form.age} onChange={e=>setForm({...form,age:e.target.value})}><option value="">Pilih umur</option>{[3,4,5,6,7].map(age=><option key={age} value={age}>{age} tahun</option>)}</select><div className="avatars">{AVATARS.map(a=><button type="button" key={a.id} className={`avatarBtn ${form.avatar===a.id?'active':''}`} onClick={()=>setForm({...form,avatar:a.id})}>{a.icon}</button>)}</div>{msg&&<div className="alert">{msg}</div>}<button className="primary" disabled={busy}>Simpan</button></form></Modal>}
function calcStars(correct,totalLevels){const ratio=totalLevels?correct/totalLevels:0;if(ratio>=.8)return 3;if(ratio>=.5)return 2;return 1}
const confettiBits=Array.from({length:28},(_,i)=>i);
function ChallengeExperience({challenge,choose,disabled}){const type=challenge.interaction||'tap-choice';const math=['count-choice','compare','story-math','mixed'].includes(type)||['tambah','tolak','banding','nombor-hilang','cerita','finale-matematik'].includes(challenge.skill);const spell=type==='spell-complete';const combine=type==='word-combine';const parts=combine?String(challenge.visual).split('+').map(x=>x.trim()):[];const visual=String(challenge.visual||'');const firstSpace=visual.indexOf(' ');const icon=spell&&firstSpace>0?visual.slice(0,firstSpace):'';const word=spell&&firstSpace>0?visual.slice(firstSpace+1):visual;return <div className="challengeExperience">{challenge.visualSrc?<div className="questionImageFrame"><img className="questionImage" src={challenge.visualSrc} alt=""/></div>:combine?<div className="combineVisual">{parts.map((x,i)=><React.Fragment key={i}><div className={i?'combinePart cool':'combinePart warm'}>{x}</div>{i<parts.length-1&&<b>＋</b>}</React.Fragment>)}</div>:spell?<div className="spellVisual"><div>{icon}</div><strong>{word.split('').map((c,i)=><span key={i} className={c==='_'?'missingLetter':''}>{c}</span>)}</strong></div>:<div className={`simpleVisual ${math?'mathVisual':challenge.visualType==='letter'?'letterVisual':''}`}>{challenge.visual}</div>}<h1 className="questionPrompt">{challenge.prompt}</h1><div className="answerOptions">{challenge.options.map((o,i)=><button className={spell?'answerOption spellOption':'answerOption'} key={`${o}-${i}`} disabled={disabled} onClick={()=>choose(o)}>{o}</button>)}</div></div>}
function GameScreen({game,child,level,onBack,onComplete,soundOn,onToggleSound}){const challenge=useMemo(()=>makeChallenge(game.game_key,level,child.age),[game.game_key,level,child.age]);const[result,setResult]=useState(''),[busy,setBusy]=useState(false),[countdown,setCountdown]=useState(5);const doneRef=useRef(false);useEffect(()=>{doneRef.current=false;setResult('');setCountdown(5)},[game.game_key,level]);useEffect(()=>{if(!soundOn||!challenge?.prompt||typeof window==='undefined'||!window.speechSynthesis)return;const t=setTimeout(()=>{try{window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(challenge.prompt);u.lang=challenge.language==='en'?'en-US':'ms-MY';u.rate=.88;window.speechSynthesis.speak(u)}catch{}},250);return()=>{clearTimeout(t);try{window.speechSynthesis.cancel()}catch{}}},[challenge,soundOn]);const isLast=level>=game.total_levels;const nextLevel=isLast?level:level+1;const finish=()=>{if(!result||doneRef.current)return;doneRef.current=true;onComplete(result==='correct',nextLevel,isLast)};useEffect(()=>{if(!result)return;setCountdown(5);const id=setInterval(()=>setCountdown(v=>{const next=Math.max(0,v-1);if(next===0)setTimeout(finish,0);return next}),1000);return()=>clearInterval(id)},[result]);async function choose(value){if(result||busy)return;const ok=String(value)===String(challenge.answer);playSfx(ok?'correct':'wrong',soundOn);setResult(ok?'correct':'wrong');setBusy(true);try{await api('/api/performance',{method:'POST',body:JSON.stringify({child_id:child.id,game_key:game.game_key,level,correct:ok})});await api('/api/progress',{method:'POST',body:JSON.stringify({child_id:child.id,game_key:game.game_key,level_reached:isLast?game.total_levels:nextLevel})})}catch{}finally{setBusy(false)}}const isCorrect=result==='correct';return <section className={`gameScreen level-${worldMeta[game.game_key]?.tone||'sky'}`}><div className="gameBar"><button className="levelBack" onClick={onBack}>← Level</button><div><small>{worldMeta[game.game_key]?.world}</small><b>Level {level}</b></div><button className="soundToggle" onClick={onToggleSound} aria-label={soundOn?'Tutup bunyi':'Hidupkan bunyi'}>{soundOn?'🔊 Bunyi':'🔇 Senyap'}</button></div><div className="gameProgress" aria-label={`Level ${level} daripada ${game.total_levels}`}><span style={{width:`${Math.min(100,level/(game.total_levels||1)*100)}%`}}/></div><div className="gameBoard"><ChallengeExperience challenge={challenge} choose={choose} disabled={!!result}/></div>{result&&<div className={`resultOverlay ${isCorrect?'resultCorrect':'resultWrong'}`}>{isCorrect&&<div className="confettiLayer" aria-hidden="true">{confettiBits.map(i=><i key={i} style={{'--i':i}}/>)}</div>}<div className="resultPopup"><div className="resultIcon">{isCorrect?'🎉':'🌱'}</div><h2>{isCorrect?'Betul! Hebat!':'Belum tepat'}</h2><p>{isCorrect?`Bagus, ${child.name}!`:<>Jawapan yang betul ialah <strong>{String(challenge.answer)}</strong>.</>}</p><div className="resultCountdown"><span>⏱️</span><b>{countdown}</b><small>{isLast?'ke keputusan':'ke level seterusnya'}</small></div><button onClick={finish}>{isLast?'Lihat keputusan →':`Teruskan sekarang (${countdown}) →`}</button></div></div>}</section>}
function LevelPage({game,child,onBack,onPlay,refreshKey}){const meta=worldMeta[game.game_key]||{};const missions=Array.from({length:game.total_levels||1},(_,i)=>i+1);const[reached,setReached]=useState(1);useEffect(()=>{api(`/api/progress?child_id=${child.id}`).then(d=>{const row=(d.progress||[]).find(p=>p.game_key===game.game_key);setReached(Math.max(1,row?.level_reached||1))}).catch(()=>setReached(1))},[child.id,game.game_key,refreshKey]);return <section className={`levelPage level-${meta.tone||'sky'}`}><div className="levelTopbar"><button className="levelBack" onClick={onBack}>← Dunia</button><div className="levelTitle"><span className="levelEmoji">{meta.icon||'🎮'}</span><div><h1>{meta.world||game.name_ms}</h1><p>{child.name} • Pengembaraan pembelajaran</p></div></div><div className="levelScene">{meta.scene}</div></div><div className="levelMapWrap"><div className="levelMapLabel"><b>Pilih level</b><span>Level {Math.min(reached,missions.length)} daripada {missions.length}</span></div><div className="levelTrack">{missions.map(n=>{const open=n<=reached;const completed=n<reached;const current=n===reached;const final=n===missions.length;const marker=completed?'✓':current?'★':final?'🏆':'🔒';return <div className={`levelNodeWrap ${open?'open':'locked'} ${completed?'completed':''} ${current?'current':''} ${final?'finalLevel':''}`} key={n}><button className="levelNode" disabled={!open} onClick={()=>open&&onPlay(n)} aria-label={completed?`Level ${n} selesai`:current?`Main level ${n}`:`Level ${n} dikunci`}><span>{marker}</span><b>{n}</b></button></div>})}</div></div></section>}
function ParentProgressPage({profiles,games,onBack}){
  const[rows,setRows]=useState([]),[loading,setLoading]=useState(true);
  useEffect(()=>{let alive=true;(async()=>{setLoading(true);const data=await Promise.all(profiles.map(async p=>{const perf=await api(`/api/performance?child_id=${p.id}`).catch(()=>({performance:[]}));const byKey=Object.fromEntries((perf.performance||[]).map(x=>[x.game_key,x]));const worlds=games.map(g=>{const r=byKey[g.game_key]||{};const correct=Number(r.correct_levels||0),attempted=Number(r.attempted_levels||0),stars=correct?calcStars(correct,g.total_levels):0;return{...g,correct,attempted,stars,percent:Math.round(correct/(g.total_levels||1)*100)}});return{...p,worlds,totalStars:worlds.reduce((s,w)=>s+w.stars,0),mastered:worlds.reduce((s,w)=>s+w.correct,0),possible:worlds.reduce((s,w)=>s+(w.total_levels||0),0)}}));if(alive){data.sort((a,b)=>b.totalStars-a.totalStars||b.mastered-a.mastered);setRows(data);setLoading(false)}})();return()=>{alive=false}},[profiles,games]);
  return <section className="parentProgressPage"><div className="parentProgressHead"><button className="levelBack" onClick={onBack}>← Kembali</button><div><span className="eyebrow">KAWASAN IBU BAPA</span><h1>Prestasi Anak</h1><p>Ringkasan kemajuan, bintang dan penguasaan setiap dunia.</p></div></div>{loading?<div className="state">Memuat prestasi…</div>:<><div className="rankingBoard"><h2>🏅 Ranking Keluarga</h2>{rows.map((p,i)=><div className="rankRow" key={p.id}><b>{i+1}</b><span className="rankAvatar">{avatarIcon(p.avatar)}</span><div><strong>{p.name}</strong><small>{p.mastered}/{p.possible} level dikuasai</small></div><em>{p.totalStars} 🌟</em></div>)}</div><div className="progressKids">{rows.map(p=><article className="progressKid" key={p.id}><div className="progressKidHead"><span>{avatarIcon(p.avatar)}</span><div><h2>{p.name}</h2><p>{p.age} tahun • {p.totalStars} bintang</p></div></div><div className="progressWorldGrid">{p.worlds.map(w=><div className="progressWorld" key={w.game_key}><div><b>{worldMeta[w.game_key]?.world||w.name_ms}</b><span>{w.correct}/{w.total_levels} level</span></div><strong>{w.percent}%</strong><div className="miniStars">{[1,2,3].map(n=><span key={n} className={n<=w.stars?'earned':''}>🌟</span>)}</div></div>)}</div></article>)}</div></>}</section>;
}

function ParentDashboard({parent,games,onLogout}){
  const[profiles,setProfiles]=useState([]),[loading,setLoading]=useState(true),[selected,setSelected]=useState(null),[editor,setEditor]=useState(null),[world,setWorld]=useState(null),[playing,setPlaying]=useState(null),[gate,setGate]=useState(false),[progressTick,setProgressTick]=useState(0),[soundOn,setSoundOn]=useState(()=>localStorage.getItem('kiddo_sound')!=='off'),[parentView,setParentView]=useState(''),[gateMode,setGateMode]=useState('edit'),[worldProgress,setWorldProgress]=useState({});
  const navRef=useRef({});
  async function load(){setLoading(true);try{const d=await api('/api/child-profiles');setProfiles(d.profiles||[])}finally{setLoading(false)}}
  useEffect(()=>{load()},[]);
  useEffect(()=>{if(!selected){setWorldProgress({});return}api(`/api/progress?child_id=${selected.id}`).then(d=>setWorldProgress(Object.fromEntries((d.progress||[]).map(p=>[p.game_key,p])))).catch(()=>setWorldProgress({}))},[selected?.id,progressTick]);
  useEffect(()=>{navRef.current={editor,gate,parentView,playing,world,selected}},[editor,gate,parentView,playing,world,selected]);
  function toggleSound(){setSoundOn(v=>{const next=!v;localStorage.setItem('kiddo_sound',next?'on':'off');return next})}
  function appBack(){const s=navRef.current;if(s.editor){setEditor(null);return}if(s.gate){setGate(false);return}if(s.parentView){setParentView('');return}if(s.playing){setPlaying(null);return}if(s.world){setWorld(null);return}if(s.selected){setSelected(null)}}
  useEffect(()=>{if(typeof window==='undefined')return;window.history.replaceState({kiddo:true},'');window.history.pushState({kiddo:true},'');const onPop=()=>{appBack();window.history.pushState({kiddo:true},'')};window.addEventListener('popstate',onPop);return()=>window.removeEventListener('popstate',onPop)},[]);
  async function handleComplete(ok,next,isLast){setProgressTick(x=>x+1);if(isLast){setPlaying(null)}else setPlaying(next)}
  return <div className="dashboardShell"><header className="dashHeader"><button className="brand" onClick={()=>{setPlaying(null);setWorld(null);setSelected(null)}}><span>🎈</span><b>Kiddo Adventures</b></button><div className="dashActions">{!selected&&!world&&!playing&&<button className="ghost mini logoutBtn" onClick={onLogout}>Log keluar</button>}</div></header><main className="dashboardMain">{parentView==='progress'?<ParentProgressPage profiles={profiles} games={games} onBack={()=>setParentView('')}/>:playing&&world&&selected?<GameScreen key={`${world.game_key}-${playing}`} game={world} child={selected} level={playing} onBack={()=>setPlaying(null)} onComplete={handleComplete} soundOn={soundOn} onToggleSound={toggleSound}/>:world&&selected?<LevelPage game={world} child={selected} refreshKey={progressTick} onBack={()=>setWorld(null)} onPlay={setPlaying}/>:!selected?<section className="profileStage"><div className="profileHeading"><span className="eyebrow">PILIH PEMAIN</span><h1>Siapa nak main?</h1><p>Pilih profil anak untuk sambung pengembaraan pembelajaran.</p></div>{loading?<div className="state">Memuat profil…</div>:<div className="profileGrid">{profiles.map(p=><div className="profileTile" key={p.id}><button className="profilePick" onClick={()=>setSelected(p)}><span>{avatarIcon(p.avatar)}</span><b>{p.name}</b><small>{p.age} tahun</small><em>Jom main →</em></button></div>)}{profiles.length<5&&<button className="profileAdd" onClick={()=>setEditor('new')}><span>＋</span><b>Tambah Anak</b><small>Maksimum 5 profil</small></button>}</div>}</section>:<><section className="kidHero"><div className="kidHeroIdentity"><div className="kidBigAvatar">{avatarIcon(selected.avatar)}</div><div><span className="eyebrow">PROFIL AKTIF</span><h1>Jom main, {selected.name}!</h1><p>{selected.age} tahun • Pilih dunia untuk sambung pengembaraan.</p></div></div><div className="kidHeroActions"><button className="ghost" onClick={()=>{setGateMode('edit');setGate(true)}}>✏️ Urus Profil</button><button className="ghost" onClick={()=>setSelected(null)}>Tukar Profil</button><button className="parentProgressBtn" onClick={()=>{setGateMode('progress');setGate(true)}}>Prestasi</button></div></section><section className="dashWorlds"><div className="dashSectionTitle"><div><span className="eyebrow">DUNIA PEMBELAJARAN</span><h2>Pilih pengembaraan</h2></div><span className="statusPill">6 dunia • 180 level</span></div><div className="worldGrid dashGrid">{games.map((g,i)=>{const m=worldMeta[g.game_key]||{};const reached=Math.max(1,Number(worldProgress[g.game_key]?.level_reached||1));const completed=Math.max(0,Math.min(g.total_levels,reached-1));const percent=Math.round(completed/(g.total_levels||1)*100);return <button className={`worldCard ${m.tone}`} key={g.id} onClick={()=>setWorld(g)}><span className="worldNo">{String(i+1).padStart(2,'0')}</span><div className="worldEmoji">{m.icon||'🎮'}</div><h3>{m.world||g.name_ms}</h3><p>{m.tag||g.name_en}</p><div className="worldCardProgress"><span><i style={{width:`${percent}%`}}/></span><small>{completed}/{g.total_levels} level selesai</small></div><div className="worldFoot"><span>{g.total_levels} misi</span><b>▶ Main</b></div></button>})}</div></section></>}</main>{gate&&<ParentGate parent={parent} onClose={()=>setGate(false)} onVerified={()=>{setGate(false);if(gateMode==='progress')setParentView('progress');else setEditor(selected)}}/>}{editor&&<ProfileEditor parent={parent} profile={editor==='new'?null:editor} onClose={()=>setEditor(null)} onSaved={load}/>}</div>
}
function AgentModal({onClose}){const[form,setForm]=useState({name:'',email:'',phone:'',bank_name:'',bank_account:''}),[msg,setMsg]=useState(''),[done,setDone]=useState(null);async function submit(e){e.preventDefault();setMsg('');try{setDone(await api('/api/agents',{method:'POST',body:JSON.stringify(form)}))}catch(err){setMsg(err.message)}}return <Modal title="Jadi Agent Kiddo" onClose={onClose}>{done?<div className="success"><h3>Berjaya 🎉</h3><div className="codeBox">{done.agent_code}</div></div>:<form className="form" onSubmit={submit}><input required placeholder="Nama" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><button className="primary">Daftar Agent</button></form>}</Modal>}
function Landing({onLogin,onAgent,onBuy}){return <main><header><button className="brand"><span>🎈</span><b>Kiddo Adventures</b></button><nav><button onClick={onAgent}>Jadi Agent</button><button className="parent" onClick={onLogin}>Log Masuk</button></nav></header><section className="hero"><div className="heroCopy"><span className="pill">BELAJAR • MAIN • TEROKA</span><h1>Bukan sekadar level.<br/><em>Satu dunia untuk diteroka.</em></h1><p>Kiddo Adventures menukar pembelajaran awal menjadi pengembaraan.</p><div className="heroActions"><button className="cta" onClick={onBuy}>Buka Semua Dunia • RM{PRICE}</button></div></div><div className="storybook"><div className="planet">🌈</div><div className="rocket">🚀</div></div></section></main>}
function App(){const[parent,setParent]=useState(()=>{try{return JSON.parse(localStorage.getItem('kiddo_parent'))}catch{return null}}),[games,setGames]=useState([]),[modal,setModal]=useState(null),[refCode,setRefCode]=useState('');useEffect(()=>{const ref=new URLSearchParams(location.search).get('ref');if(ref)setRefCode(ref.toUpperCase());api('/api/games').then(d=>setGames((d.games||[]).map(g=>({...g,total_levels:30})))).catch(()=>{})},[]);function loginDone(d){setParent(d);setModal(null)}function logout(){localStorage.removeItem('kiddo_parent');setParent(null)}if(parent)return <ParentDashboard parent={parent} games={games} onLogout={logout}/>;return <><Landing onLogin={()=>setModal('auth')} onAgent={()=>setModal('agent')} onBuy={()=>setModal('auth')}/>{modal==='auth'&&<AuthModal onClose={()=>setModal(null)} onLogin={loginDone} refCode={refCode}/>} {modal==='agent'&&<AgentModal onClose={()=>setModal(null)}/>}</>}
createRoot(document.getElementById('root')).render(<App/>);
